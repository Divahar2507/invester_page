#! db-postgres_data_v3-persistentVolumeClaim.yaml
# Generated code, do not edit
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: db-postgres-data-v3
    namespace: invester-dev
    labels:
        com.docker.compose.service: db
        com.docker.compose.volume: postgres_data_v3
spec:
    accessModes:
        # TODO would need to check which services use this volume
        - ReadWriteOnce
    volumeMode: Filesystem
    resources:
        requests:
            storage: 100Mi
